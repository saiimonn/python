areas = [11.25, 18.0, 20.0, 10.75, 9.50]

for idx, area in enumerate(areas):
    print("room " + str(idx) + ": " + str(area))

# **enumerate** lets you print two different values in the list (i.e. its index and the value itself)